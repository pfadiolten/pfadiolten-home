.simple-calendar
  - params[:calendar] = calendar_types.week
  = render 'simple_calendar/calendar_buttons'
  .calendar-heading.gap-bottom-1
    .row
      .col-xs-4.text-right
        = link_to fa_icon('chevron-circle-left', class: 'fa-2x'), calendar.url_for_previous_view

      .col-xs-4.text-center
        - if calendar.number_of_weeks == 1
          %span.calendar-title
            = "#{t('simple_calendar.names.week')} #{calendar.week_number}, #{start_date.year}"
        - else
          %span.calendar-title
            Week #{calendar.week_number} - #{calendar.end_week}
      .col-xs-4.text-left
        = link_to fa_icon('chevron-circle-right', class: 'fa-2x'), calendar.url_for_next_view
  %table.table.table-striped
    %thead
      %tr
        - date_range.slice(0, 7).each do |day|
          %th= t('date.abbr_day_names')[day.wday]
    %tbody
      - date_range.each_slice(7) do |week|
        %tr
          - week.each do |day|
            = content_tag :td, class: calendar.td_classes_for(day) do
              - if defined?(Haml) && respond_to?(:block_is_haml?) && block_is_haml?(block)
                - capture_haml(day, sorted_events.fetch(day, []), &block)
              - else
                - block.call day, sorted_events.fetch(day, [])
