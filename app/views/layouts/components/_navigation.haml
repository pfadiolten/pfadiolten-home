%nav.navbar.navbar-default.navbar-fixed-top{ role: 'navigation' }
  .container
    .navbar-header
      = render 'layouts/components/navigation/social', add_class: 'hidden-md-up'
      %button.navbar-toggle.collapsed{ data: { target: '#main-navbar', toggle: 'collapse', type: 'button' } }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand.brand-name{ href: root_path } Pfadi Olten
      %a.navbar-brand.brand-logo{ href: root_path }
        %img{ src: image_path('logos/pfadi_olten-text.svg') }

    #main-navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav
        - nav_entries.each do |controller|
          %li{ class: ('active' if current_page?(controller: controller)) }
            %a{ href: url_for(controller: controller, action: 'index') }
              = t("activerecord.models.#{controller.to_s.singularize}.other")

      %ul.nav.navbar-nav.navbar-right
        %li.hidden-sm.gap-right-2
          = render 'layouts/components/navigation/social'

        - unless user_signed_in?
          %li{ class: ('active' if current_page?(controller: 'sessions', action: 'new')) }
            %a{ href: new_user_session_path }
              = fa_icon('sign-in')
              &nbsp;
              = t('devise.sessions.new.title')
        - else
          %li
            %a{ href: destroy_user_session_path, data: { method: 'delete' } }
              = current_user.scout_name
              &nbsp;
              = fa_icon('sign-out')