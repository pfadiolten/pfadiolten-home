version: '3.5'
services:
  web:
    image: pfadiolten-home:dev
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    environment:
      - RAILS_ENV=development
      - http_proxy
      - https_proxy
      - no_proxy
      - PFADIOLTEN_OSM_ACCESS_TOKEN
      - PFADIOLTEN_INSTAGRAM_CLIENT_ID
      - PFADIOLTEN_INSTAGRAM_CLIENT_SECRET
      - PFADIOLTEN_INSTAGRAM_ACCESS_TOKEN
    volumes:
      - .:/pfadiolten-home
    ports:
      - '3000:3000'
    depends_on:
      - db
  db:
    image: postgres
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data:
    external:
      name: pfadiolten-home.data

# docker volume create --name pfadiolten-home.data
# docker-compose run web <rails command>